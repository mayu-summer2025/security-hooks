# security-hooks
Checks for leaked api keys, or tokens in commits

## Required Security Setup

1. Run the following command to install the security hooks setup tool:

   ```bash
   curl -s https://raw.githubusercontent.com/mayu-summer2025/security-hooks/main/global-hooks-setup.sh | bash